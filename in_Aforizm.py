import class_construct
def aforizm_in(container,i,part):
    container[i] = class_construct.Aforizm()
    container[i].index = part[0]
    container[i].country = part[1]
    container[i].content = part[2]